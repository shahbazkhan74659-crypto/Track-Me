# Current Tasks

## Active

None actively in progress.

## Next

The owner locked the full roadmap on 2026-09-04, renumbered once that day (injecting Phase 14 "Mobile View / Mobile Responsive") and again on 2026-09-05 (injecting Phase 13 "Date Entry Modal on Frontend", split out of Phase 10 — see `PHASES.md`, `DECISIONS.md`). The roadmap is now Phase 0–20. [Phase 1] "Local PostgreSQL Setup", [Phase 2] "Temporary Backend Test Page", and [Phase 3] "Login Backend (no signup)" are all complete (2026-09-04) — see Completed below. The one login account (username "Shahbaz") has been seeded via `scripts/seed-user.mjs`. [Phase 4] "Salary Setup Modal Backend" is also complete (2026-09-04) — see Completed below. [Phase 5] "Calendar Backend" is also complete (2026-09-05) — see Completed below. [Phase 6] "Clickable Date Modal Backend" is also complete (2026-09-05) — see Completed below. [Phase 7] "Salary / Advance / Attendance Calculation Backend" is also complete (2026-09-05) — see Completed below. [Phase 8] "Full Backend Testing & Temporary Page Removal" is also complete (2026-09-05) — see Completed below. [Phase 9] "React Frontend: Blank Dark-Themed Shell" is also complete (2026-09-05) — see Completed below. [Phase 10] "Calendar on Frontend", [Phase 11] "Stat Cards on Frontend", [Phase 12] "Profile & Salary Setup on Frontend", and [Phase 13] "Date Entry Modal on Frontend" are all complete (2026-09-05) — see Completed below. [Phase 14] "Wire Frontend to Backend" is also complete (2026-09-05) — see Completed below. This included a real login page (a gap discovered when starting the phase — no phase had ever built one; the owner chose to fold it into Phase 14 rather than split it out) and a new `components/AttendanceApp.tsx` coordinator connecting every component to the real backend; `GET /api/calendar` is deliberately not called (see `DECISIONS.md`). **Real, authenticated verification (logging in, saving a real entry/rate, confirming live recalculation) still needs the owner's manual browser check** — this could not be done from this session (no access to the real account's password, and creating a second test account would add a permanent row to the real database without sign-off). This is more important than any prior phase's visual-pass caveat, since Phase 14 is the first phase touching real persisted data. [Phase 15] "Mobile View / Mobile Responsive" is also complete (2026-09-05) — see Completed below. [Phase 16] "End-to-End Testing & Production Readiness" is **complete** (2026-09-05 — see Completed below): Playwright E2E + component test suites, login rate-limiting and baseline security headers, an env var audit with a fail-fast `DATABASE_URL` check, styled error/not-found pages, Node version pinning, and crawler exclusion — verified under both `next dev` and a real `next build && next start`. [Phase 17] "Neon PostgreSQL Setup" is also complete (2026-09-05) — see Completed below. [Phase 18] "Deployment on Render" is **partially complete** (2026-09-05, owner gave the go-ahead — see Completed below): deploy-readiness (`render.yaml`, `/api/health`, a tightened `engines.node` upper bound) is built and locally verified; the live deploy itself (push to GitHub, connect Render, first build) is still owner-manual, blocked by `CLAUDE.md`'s no-commit/no-push rule and this session having no Render dashboard access.

**Bug fix (2026-09-05, post-Phase 11):** the owner caught the page not scrolling once the calendar + stat cards made the content taller than the viewport — a side effect of Phase 9's fixed-viewport/`overflow:hidden` shell, which had no content to worry about at the time. Fixed by switching `html`/`body`/the page div to `min-height` and dropping `overflow:hidden` in favor of `overflow-x:hidden`. See `DECISIONS.md`'s "Frontend shell reverted to natural page scroll" entry and `ARCHITECTURE.md`'s Phase 9 section.

Note 2026-09-04: a first attempt to jump straight into scaffolding the Next.js project (before Phase 1's database setup) was interrupted and reverted by the owner — "we are not going to start coding yet." The owner then scoped Phase 1 down to local database setup only, separately confirmed not to start executing it yet, and then locked the full downstream roadmap (Phases 2–18) in one message.

Resolved 2026-09-04: the earlier open question — whether the deployed app needs a password/PIN access gate — is answered by the roadmap itself: [Phase 3] is a real Login/Signup Backend. See `DECISIONS.md`'s "Login/signup backend added to the roadmap" entry, which also flags that this reverses the earlier "no authentication planned" decision — `PROJECT.md` and `ARCHITECTURE.md` were updated to match.

**Reminder:** per `CLAUDE.md`'s first rule, nothing gets committed or pushed to git until the owner explicitly says so — this applies to all future work, not just documentation.

## Blocked

None.

## Completed

- [x] [Phase 0a] Define core feature set: calendar attendance entry (Present/Half-Day/Leave + optional per-date advance salary amount), live stat cards (Earned So Far, Advance Taken, Net Payable), Salary Setup (per-day rate, everything else auto-calculated) — 2026-09-04
- [x] [Phase 0b] Build interactive Claude Design Components prototype (`prototype/Main.dc.html`) covering the full Home page flow, with working calendar navigation, date-entry modal, live stat calculations, and salary setup — 2026-09-04
- [x] [Phase 0b] Background review of prototype source found and fixed two real bugs: the "today" ring on the calendar never rendered (`todayKey` was read from the wrong object), and a ₹0 advance was silently treated as no advance (truthiness check on the amount instead of a dedicated flag) — 2026-09-04
- [x] [Phase 0b] Convert prototype to a dark theme (near-black background, brightened indigo accent and status colors for dark-background contrast), layout/structure unchanged — 2026-09-04
- [x] [Phase 0b] Owner reviewed and approved this version of the design and styling — 2026-09-04
- [x] [Phase 0a] Lock production stack: Next.js (TypeScript), React frontend with AJAX-driven updates, PostgreSQL (local dev / Neon production), Render (free tier), UptimeRobot keep-alive — 2026-09-04
- [x] Split Phase 1 to local PostgreSQL setup only, and decide its install method (native Windows, via `winget`) — 2026-09-04
- [x] Lock the full downstream roadmap, Phase 2 through Phase 18 — see `PHASES.md` — 2026-09-04
- [x] [Phase 1] Local PostgreSQL Setup: found a pre-existing local PostgreSQL 18 service already running (not the planned fresh v17 install), created the `trackme_dev` database on it, and verified connectivity to both the server and the new database via `psql` — 2026-09-04. See `DECISIONS.md`'s "Reused pre-existing local PostgreSQL 18" entry.
- [x] [Phase 2] Temporary Backend Test Page: scaffolded the production Next.js/TypeScript project at repo root (App Router, ESLint, no database wiring yet) and added one bare test page; verified `npm install`, `npm run build`, `npm run lint`, and the dev server all work — 2026-09-04. See `ARCHITECTURE.md`'s "Production App (Phase 2 scaffold)" section.
- [x] [Phase 3] Login Backend (no signup): created a scoped `trackme_app` Postgres role and `users`/`sessions` schema, built login/logout/session API routes with bcrypt + DB-backed sessions, seeded the single account via `scripts/seed-user.mjs`, and verified the full flow end-to-end (correct login, rejected wrong password, logout clearing both cookie and DB row) — 2026-09-04. See `ARCHITECTURE.md`'s "Login Backend (Phase 3 scaffold)" section and `DECISIONS.md`'s two Phase 3 entries.
- [x] Injected new Phase 14 "Mobile View / Mobile Responsive" into the roadmap; renumbered old Phase 14–18 to 15–19 — 2026-09-04. See `PHASES.md`, `DECISIONS.md`'s "Phase 14 injected for mobile responsiveness" entry.
- [x] [Phase 4] Salary Setup Modal Backend: added `users.per_day_salary`, a reusable `requireAuth` helper, auth-gated `GET`/`PUT /api/salary` with input validation, and a test-page Salary Setup section; verified 401-when-logged-out, 400-on-invalid-input, and persistence end-to-end — 2026-09-04. See `ARCHITECTURE.md`'s "Salary Setup Backend (Phase 4)" section.
- [x] [Phase 5] Calendar Backend: added `lib/calendar.ts` (Asia/Kolkata "today" via `Intl.DateTimeFormat`, UTC-anchored leap-year-correct calendar math, 42-cell Sunday-first month grid), an auth-gated `GET /api/calendar` route with `year`/`month` query-param validation, and a test-page Calendar section; verified 401-when-logged-out, 400-on-invalid-input, leap-year/day-count correctness, Dec/Jan adjacent-month rollover, and IST-vs-server-timezone independence (via a forced `TZ=UTC` check) — 2026-09-05. See `ARCHITECTURE.md`'s "Calendar Backend (Phase 5)" section and `DECISIONS.md`'s Asia/Kolkata entry.
- [x] [Phase 6] Clickable Date Modal Backend: added an `entries` table (`db/schema.sql`, applied by the owner via `psql` along with `trackme_app` grants) and an auth-gated `GET`/`PUT`/`DELETE /api/entries` route reproducing the prototype's date-entry-modal invariants exactly (status required to save, `advanceOn` a dedicated boolean independent of the amount, leap-year-correct day-range validation via `lib/calendar.ts`), plus a clickable Date Entry section on the test page; verified end-to-end via direct API calls — save/update/clear, the ₹0-advance regression case, month rollover, all validation 400s, and 401-when-logged-out — 2026-09-05. See `ARCHITECTURE.md`'s "Clickable Date Modal Backend (Phase 6)" section and `DECISIONS.md`.
- [x] [Phase 7] Salary / Advance / Attendance Calculation Backend: added a read-only, auth-gated `GET /api/summary` route computing Earned So Far / Advance Taken / Net Payable via a single SQL `FILTER`-clause aggregation over `entries` plus `users.per_day_salary`, reproducing the prototype's exact formula and handling the nullable per-day rate (day counts/advance always numeric, earned/net `null` but still 200 when no rate is set); added a Summary section to the test page that recalculates live after saving/clearing an entry or changing the rate, not just on month navigation; verified with a hand-calculated case (₹800 rate, 3 present/2 half/1 leave/₹500 advance → earned 3200, net 2700), adjacent-month isolation, all validation 400s, and 401-when-logged-out — 2026-09-05. See `ARCHITECTURE.md`'s "Salary / Advance / Attendance Calculation Backend (Phase 7)" section and `DECISIONS.md`.
- [x] [Phase 8] Full Backend Testing & Temporary Page Removal: wrote `scripts/test-backend.mjs` (plain Node ESM, no new dependencies) to exercise every Phase 3–7 route end-to-end against the live dev server, safely, via a dedicated fake test year and salary-value capture/restore; ran it twice with all 73 assertions passing both times, confirmed via `psql` snapshots that the real account's `per_day_salary` and current-month entries were unchanged; then deleted the Phase 2 temporary `app/page.tsx` test harness and corrected `app/layout.tsx`'s stale metadata; confirmed `/` 404s while `/api/*` routes and `npm run build`/`npm run lint` all remain clean — 2026-09-05. See `ARCHITECTURE.md`'s "Full Backend Testing & Temporary Page Removal (Phase 8)" section and `DECISIONS.md`.
- [x] [Phase 9] React Frontend: Blank Dark-Themed Shell: recreated `app/page.tsx` as a genuinely blank dark shell fixed exactly to the viewport, matching the prototype's page background, added `lib/theme.ts` (a minimal, incrementally-extended color-constants module) and `app/globals.css` (base reset), and wired up Manrope/Work Sans via `next/font/google` in `app/layout.tsx`; confirmed via grep that no frontend file references `fetch`/`lib/db`/`lib/auth`/`lib/calendar`/`/api/` — the frontend stays fully disconnected from the backend per the owner's instruction until Phase 13; also disabled Next.js's dev-mode indicator (`devIndicators: false`) and fixed a white-background/unwanted-scroll bug the owner caught via screenshots (pinned `html`/`body` to `height:100%; overflow:hidden`, exact `height:100vh` on the page shell); `npm run build`/`npm run lint` both pass clean — 2026-09-05. See `ARCHITECTURE.md`'s "Frontend Shell (Phase 9)" section and `DECISIONS.md`.
- [x] [Phase 10] Calendar on Frontend: asked the owner whether the clickable date-entry modal belonged in this phase; the owner had it split into a new [Phase 13] "Date Entry Modal on Frontend" instead, with old Phases 13–19 renumbered to 14–20 (see `DECISIONS.md`). Built `components/CalendarCard.tsx` (month/year nav header, weekday row, 42-cell grid, legend — cells not yet clickable), reusing `lib/calendar.ts`'s existing `getTodayIST`/`buildMonthGrid`/`normalizeYearMonth` rather than reimplementing calendar math, and extended `lib/theme.ts` with only the palette entries this component needs; added two small `:hover`-only CSS classes to `app/globals.css` for the chevron buttons and "Today" link (inline styles can't express `:hover`, matching the prototype's own pattern of moving hover-styled elements into real CSS); rendered inside `app/page.tsx`. `npm run build`/`npm run lint` pass clean; grep confirmed no `fetch`/`lib/db`/`lib/auth`/`/api/` references; the running dev server's rendered HTML confirmed the correct current month, 42 cells, and the full legend — 2026-09-05. Browser-based visual/interactive verification (hover, click navigation, today ring, adjacent-month dimming) was not done — Chrome browser automation wasn't connected this session — so the owner should eyeball it in a browser before treating the UI as final. See `PHASES.md`'s Phase 10 entry and `DECISIONS.md`.
- [x] [Phase 11] Stat Cards on Frontend: built `components/StatCards.tsx` (Earned So Far / Advance Taken / Net Payable, styled per the prototype, placed above the calendar card) rendering the same "unset rate" placeholder state `GET /api/summary` already defines (Phase 7) — day counts and advance-taken as real zeros, earned/net shown as "—" with "Set a rate to see earnings", since no rate can be entered (Phase 12) or attendance logged (Phase 13) yet. Added `lib/format.ts`'s `formatINR` as the first shared currency formatter (Phase 12/13 to reuse); extended `lib/theme.ts` with 4 new tokens for the Net Payable card's accent panel, reusing the existing `statusLeave` token for the Advance Taken card's number. `npm run build`/`npm run lint` pass clean; grep confirmed no real `fetch`/`lib/db`/`lib/auth`/`/api/` usage; the running dev server's rendered HTML confirmed all three cards and the placeholder copy — 2026-09-05. Browser-based visual verification not done this session (Chrome automation unavailable), same caveat as Phase 10. See `PHASES.md`'s Phase 11 entry and `ARCHITECTURE.md`'s "Stat Cards Frontend (Phase 11)" section.
- [x] [Phase 12] Profile & Salary Setup on Frontend: asked the owner whether the Salary Setup modal belonged in this phase (same gap Phase 10 had with the date-entry modal); the owner said include it. Built `components/ProfileBadge.tsx` (static branding badge) and `components/SalarySetupControl.tsx` (per-day-salary label, Salary Setup button, and its modal — local `perDaySalary`/`modalOpen`/`draft` state only), rendered as a header row above the stat cards in `app/page.tsx`. Save is disabled unless the input is a finite number > 0, previewing `PUT /api/salary`'s existing validation (Phase 4) rather than the prototype's unvalidated save. Added `.btn-outline`/`.btn-primary` CSS classes and the number-input spin-button override to `app/globals.css`, and one new `inputPrefixBackground` token to `lib/theme.ts`. Deliberately not cross-wired to `StatCards` — setting a rate here doesn't change the stat cards yet, that's Phase 14. `npm run build`/`npm run lint` pass clean; grep confirmed no real `fetch`/`lib/db`/`lib/auth`/`/api/` usage; the running dev server's rendered HTML confirmed "Attendance", "Personal record", "Salary Setup", and "Not set" — 2026-09-05. Modal open/close/save interactivity wasn't checked in a browser this session (Chrome automation unavailable), same caveat as Phases 10–11. See `PHASES.md`'s Phase 12 entry and `ARCHITECTURE.md`'s "Profile & Salary Setup Frontend (Phase 12)" section.
- [x] [Phase 13] Date Entry Modal on Frontend: made `components/CalendarCard.tsx`'s current-month cells clickable, added local in-memory `entries` state (keyed exactly like the prototype, field names matching `app/api/entries/route.ts`'s real shape for an easy Phase 14 swap), and built `components/DateEntryModal.tsx` (status buttons, advance checkbox + amount, Save/Clear, keyed by date so its draft state resets cleanly per-date). Save is disabled without a status, and also disabled if a typed advance amount isn't a finite number ≥ 0 (a blank amount still counts as 0, preserving the ₹0-advance-counts-as-taken invariant) — previewing `PUT /api/entries`'s real validation, same spirit as Phase 12. Added 5 new `lib/theme.ts` tokens (soft status colors + disabled-control colors) and one `.status-btn:hover` CSS rule. Deliberately not cross-wired to `StatCards` — entries created here don't change the stat cards, that's Phase 14. `npm run build`/`npm run lint` pass clean; grep confirmed no real `fetch`/`lib/db`/`lib/auth`/`/api/` usage; the running dev server confirmed 42 grid cells with exactly the current month's day count marked clickable, and confirmed the modal renders nothing until a cell is clicked — 2026-09-05. This phase is almost entirely interactive and couldn't be verified in a real browser this session (Chrome automation unavailable) — a manual pass matters more here than for Phases 10–12. See `PHASES.md`'s Phase 13 entry and `ARCHITECTURE.md`'s "Date Entry Modal Frontend (Phase 13)" section.
- [x] [Phase 14] Wire Frontend to Backend: discovered no phase had ever built a frontend login page (a real, blocking gap — every `/api/*` route 401s without a session cookie); asked the owner, who chose to fold a minimal one into this phase. Made `app/page.tsx` an async Server Component checking the session directly (`lib/auth.ts`'s `getSessionUser`, no client round-trip) and rendering either the new `components/LoginGate.tsx` or the authenticated app; added a minimal "Log out" link. Built `components/AttendanceApp.tsx`, the coordinator Phases 10–13 deferred to: owns `view`/`perDaySalary`/`entries`/`summary` state and every fetch/mutation (`GET`/`PUT /api/salary`, `GET`/`PUT`/`DELETE /api/entries`, `GET /api/summary`), converting `CalendarCard`, `StatCards`, and `SalarySetupControl` from self-contained placeholders into controlled components; `DateEntryModal` and `SalarySetupControl` gained `saving`/`error` state for their now-async saves. Deliberately does not call `GET /api/calendar` — the grid is already correct client-side (see `DECISIONS.md`). `npm run build`/`npm run lint` pass clean (`/` now correctly dynamic since it reads cookies); grep confirmed the real endpoints are called and `/api/calendar` isn't; the login form was confirmed to render when logged out — 2026-09-05. **Real, authenticated end-to-end verification needs the owner's manual browser check** — not done this session (no access to the real account's password; creating a test account would alter the real database without sign-off). See `PHASES.md`'s Phase 14 entry, `ARCHITECTURE.md`'s "Login & Data Wiring (Phase 14)" section, and `DECISIONS.md`'s two Phase 14 entries.
- [x] [Phase 15] Mobile View / Mobile Responsive: surveyed every layout-relevant frontend file and confirmed via the rendered HTML that Next.js 16 already auto-injects the viewport meta tag (no gap there). Fixed the real problems — `StatCards.tsx`'s fixed 3-column grid, `CalendarCard.tsx`'s fixed cell height/card padding/month-label width, two non-wrapping header flex rows (`AttendanceApp.tsx`, `SalarySetupControl.tsx`), and `.chevron-btn`'s under-sized touch target — by adding one `@media (max-width: 640px)` block to `app/globals.css` and moving each affected static style into a new CSS class (`.app-shell`, `.stat-cards-grid`, `.stat-value`, `.calendar-card`, `.calendar-cell`, `.month-label`), extending Phase 10's existing "`:hover` needs a real CSS class" pattern to media queries rather than introducing a CSS framework or JS-based viewport detection. Also added `maxHeight:"90vh"; overflowY:"auto"` to both modals as a defensive fix for short viewports. `DateEntryModal`, `SalarySetupControl`'s modal shell, `LoginGate`, and `ProfileBadge` were already responsive-safe and needed no structural change. `npm run build`/`npm run lint` pass clean; grep confirmed no removed inline property was left duplicated — 2026-09-05. Browser-based visual verification at real phone widths wasn't done this session (Chrome automation unavailable) — the owner should check the live app on a phone or a resized/DevTools-emulated narrow window before treating it as final. See `PHASES.md`'s Phase 15 entry, `ARCHITECTURE.md`'s "Mobile Responsiveness (Phase 15)" section, and `DECISIONS.md`.
- [x] [Phase 16, testing half] Playwright E2E + component test suites: asked the owner how authenticated E2E flows should log in and how mutating tests should avoid corrupting real data; the owner chose a dedicated test-only account (`e2e-test-user`, seeded via `scripts/seed-user.mjs`, credentials in gitignored `.env.test.local`) and Phase 8's data-safety pattern. Built `e2e/` (12 tests: login gate, invalid credentials, unauthenticated 401s, logout, Salary Setup validation/persistence, calendar save/₹0-advance/clear, the earned/advance/net formula against known inputs, and a mobile-emulated project checking Phase 15's reorder + touch targets) and `tests-ct/` (19 tests: `CalendarCard`, `DateEntryModal`, `StatCards`, `SalarySetupControl`, `ProfileBadge` mounted in isolation via `@playwright/experimental-ct-react`, no server/database — `AttendanceApp`/`LoginGate` excluded since their `next/navigation` usage needs a real Next.js router context the CT harness doesn't provide, covered by E2E instead). Added `aria-label`s to the login/salary/advance-amount inputs, `role="dialog"` to both modals (fixes a real Playwright strict-mode collision between the calendar legend's "Present"/"Leave" text and the modal's identical status-button labels), and a `data-testid` per current-month calendar cell — genuine accessibility improvements, not test-only scaffolding. All 12 E2E + 19 CT tests pass, re-run twice; one real test-authoring bug was caught this way (a test assumed the Salary Setup modal always opens blank, which broke once a prior run had already saved a rate for the test account) and fixed. `npm run lint`/`npm run build` pass clean (had to add `playwright/.cache/` to `eslint.config.mjs`'s ignore list and to `.gitignore` alongside `playwright-report/`/`test-results/`/`tests-ct/__snapshots__/`) — 2026-09-05. **Phase 16's production-readiness half had not been started as of this entry** — see the next entry below for what's since been done. See `PHASES.md`'s Phase 16 entry, `ARCHITECTURE.md`'s "Testing (Phase 16)" section, and `DECISIONS.md`.
- [x] [Phase 16, production-readiness half — started] Login rate limiting and baseline security headers: discussed the app's actual security needs with the owner (a small, single/two-user personal tool, not a target justifying a WAF/CSP/compliance tooling) and agreed on two concrete, cheap fixes. Added `lib/rateLimit.ts` (in-memory, per-IP, 5 failed attempts per 15-minute window, resets on success) wired into `app/api/auth/login/route.ts` — closes a real brute-force risk given the app is reachable by URL with a fixed, guessable username and had no attempt throttling before this. Added `X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`, `Permissions-Policy`, and `Strict-Transport-Security` response headers via `next.config.ts`'s `headers()`. Verified directly against the running dev server (not just via build/lint): all five headers present on a real response, and a scripted 6-failed-login run confirmed attempts 1–5 return 401 and the 6th returns 429 — then restarted the dev server afterward since that same test run had exhausted the shared "unknown"-IP bucket local dev requests fall into (no reverse proxy in front of `next dev` to set `x-forwarded-for`), which would otherwise have locked out the owner's own next login attempt for 15 minutes. `npm run lint`/`npm run build` and the full E2E suite all still pass — 2026-09-05. **Still open in this phase's production-readiness half:** env var audit, error-page polish, and anything else it still needs. See `PHASES.md`'s Phase 16 entry, `ARCHITECTURE.md`'s "Production Readiness (Phase 16)" section, and `DECISIONS.md`.
- [x] [Phase 16, production-readiness half — finished] Env var audit, error pages, and real production-mode verification: confirmed (via grep, not assumption) `DATABASE_URL` and `NODE_ENV` are the only env vars this app reads anywhere, and added a fail-fast check in `lib/db.ts` so a missing `DATABASE_URL` throws a clear error instead of a cryptic `pg` failure later; separately verified from `pg-connection-string`'s own source that `pg` already parses `sslmode` from a connection string automatically, so Neon (Phase 17) needs no code change here. Added `app/not-found.tsx`, `app/error.tsx`, `app/global-error.tsx` (styled to match the app's dark theme) — checked this Next.js version's actual docs first and found `error.tsx`/`global-error.tsx`'s reset callback is named `retry` here, not the `reset` from older versions. Also added `package.json`'s `engines.node` (`>=20.9.0`, matching Next's own floor) and `app/robots.ts` + a `robots:{index:false,follow:false}` metadata entry (private app, no reason to be indexed). Ran `npm run build && npm run start` — this project's first real production-mode test, everything before this was only ever checked under `next dev` — and confirmed directly against it: all 5 security headers present, the custom 404 returns a real 404 status, `robots.txt` serves correctly, and a full login→authenticated-request→logout→401 cycle works with the session cookie correctly marked `Secure` (checked via curl's cookie jar). Then ran the full E2E suite against that real production server — all 12 pass. `npm run lint`/`npm run build` clean; the full E2E (12) + CT (19) suites re-verified against dev mode afterward, all still passing — 2026-09-05. **Phase 16 is now fully complete**, both halves. See `PHASES.md`'s Phase 16 entry, `ARCHITECTURE.md`'s "Production Readiness (Phase 16)" section, and `DECISIONS.md`.
- [x] [Phase 17] Neon PostgreSQL Setup: the owner provisioned the Neon project/database and supplied the pooled connection string (`neondb_owner` role). Verified connectivity directly (`SELECT version()` → PostgreSQL 18.6, compatible with local dev's 18.4), then applied `db/schema.sql` (`scripts/apply-neon-schema.mjs`) and confirmed all three tables exist via `information_schema.tables`. Following Phase 3's local precedent, created a dedicated least-privilege `trackme_app` role on Neon (`scripts/provision-neon-role.mjs`) rather than using the owner role for the app — CRUD on `users`/`sessions`/`entries` plus sequence usage only — and verified it genuinely can't run DDL (`scripts/verify-neon-app-role.mjs`: a `CREATE TABLE` attempt was rejected). Stored the scoped role's `DATABASE_URL` in a new gitignored `.env.production.local` (confirmed via `git check-ignore`) rather than `.env.local`, so `next dev` keeps using local Postgres while `next build`/`next start` and a future Render deploy pick up Neon automatically. The Neon owner-role credential was never written to any file left on disk. — 2026-09-05. See `PHASES.md`'s Phase 17 entry, `ARCHITECTURE.md`'s "Neon PostgreSQL Setup (Phase 17)" section, and `DECISIONS.md`'s "Dedicated least-privilege `trackme_app` role on Neon" entry.
- [x] [Phase 18, deploy-readiness half] Render Blueprint, health check, Node version upper bound: the owner said "Phase 19" but described Phase 18's objective — confirmed which phase before starting. Added `render.yaml` (a Render Blueprint: `type: web`, `runtime: node`, `plan: free`, `region: oregon` to co-locate with Neon's `us-west-2` pooler, `buildCommand`/`startCommand` matching existing `package.json` scripts, `healthCheckPath: /api/health`, `NODE_ENV=production`, `DATABASE_URL` marked `sync: false` so it's prompted for in Render's dashboard rather than committed). Added `app/api/health/route.ts`, an unauthenticated, database-free `GET` returning `{"status":"ok"}`, for Render's own health check and Phase 19's planned UptimeRobot target. Tightened `package.json`'s `engines.node` from `>=20.9.0` to `>=20.9.0 <25` after Render's own docs flagged the open-ended range as an unannounced-major-version-bump risk. Verified two platform assumptions directly instead of assuming them: read this Next.js version's own CLI docs and `node_modules/next/dist/bin/next`'s source to confirm `next start` reads Render's injected `PORT` natively and defaults `NODE_ENV` to `"production"` on its own; then ran a real `npm run build && PORT=3100 npm run start` and `curl`-verified the custom port was respected, `/api/health` returned `200 {"status":"ok"}`, and all five Phase 16 security headers were still present. `npm run lint`/`npm run build` both pass clean — 2026-09-05. **Not done, left for the owner:** committing/pushing this work, creating the Render web service from this repo (`origin` already points at `github.com/shahbazkhan74659-crypto/Track-Me`), pasting the Neon `DATABASE_URL` into Render's prompted secret field, and the first live deploy. See `PHASES.md`'s Phase 18 entry, `ARCHITECTURE.md`'s "Render Deployment (Phase 18)" section, and `DECISIONS.md`.
